import { styled } from "@mui/material";

export const HeaderLink = styled("a")({
  color: "#fff",
  textDecoration: "none",
});

export const MenuList = styled("ul")({
  margin: "0 0 0 10px",
  padding: "0",
  listStyle: "none",
  display: "flex",
  gap: "10px",
});
